LazyLoading ---> 
1.It is technique used to load modules only when they are needed,rather than loading them all it once during the initial application load.
2.This can significantly improve performance of large angular applications by reducing initial load.  
